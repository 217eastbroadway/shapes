How to compile (if you're on linux you will need to install raylib beforehand)

run "make"

enjoy.


note:
you will need bin/res/fonts/helvetica_neue_65.ttf in order to run the program, otherwise the randomly generated text will be printed with the default raylib font (not cool...)